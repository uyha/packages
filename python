#!/usr/bin/env bash

name=python
url='https://www.python.org/ftp/python/3.10.0/Python-3.10.0.tar.xz'

. ./make-steps

package-configure() {
  ./configure --prefix="$install_dir" \
	  --enable-optimizations \
	  CPPFLAGS="$(pkg-config --cflags zlib)" \
	  LDFLAGS="$(pkg-config --libs zlib)"
}

. ./invoke-build
